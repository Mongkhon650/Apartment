<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>ลงทะเบียน</title>
    <link rel="preconnect" href="https://fonts.gstatic.com">
    <link href="https://fonts.googleapis.com/css2?family=Pridi:wght@200;300;400;500;600;700&display=swap" rel="stylesheet">
    <link rel="stylesheet" href="/CSSFILE/bootstrap.min.css">
    <link rel="stylesheet" href="/CSSFILE/register.css">
  </head>
  <body>
    <form id="form1" action="/register" method="post">
        <div class="container">
            <label for="userType">สำหรับ : &nbsp;&nbsp;&nbsp;&nbsp;</label>
            <select name="userType" id="userType">
                <option value="general">ผู้ใช้ทั่วไป</option>
                <option value="owner">เจ้าของหอพัก</option>
            </select>
            <button type="button" onclick="nextForm(2)" style="margin-left: 20px;">ถัดไป</button>
        </div>
    </form>
    <form id="form2" action="/register" method="post" style="display: none;">
        <div class="container">
            <input type="hidden" id="userType2" name="userType">
            <div class="logo">
                <img src="/img/Apartment_Logo.png" alt="Logo">
            </div>
            <h2 class="text-center mt-2" style="background-color: #CB6CE6; border-radius: 2px;">ลงทะเบียน</h2>
           <div class="row mt-3">
                <div class="col-sm-6">
                    <input type="firstname" id="firstname" name="firstname" placeholder="ชื่อ">
                </div>
                <div class="col-sm-6">
                    <input type="lastname" id="lastname" name="lastname" placeholder="นามสกุล">
                </div>
           </div>
           <div class="col-sm-12 mt-3">
                <input type="email" id="email" name="email" placeholder="อีเมล">
           </div>
            <div class="col-sm-12 mt-3">
                <input type="text" id="username" name="username" placeholder="ชื่อผู้ใช้">
            </div>
            <div class="col-sm-12 mt-3">
                <input type="password" id="password" name="password" placeholder="รหัสผ่าน">
            </div>
            <div class="col-sm-12 mt-3">
                <input type="password" id="confirmPassword" name="confirmPassword" placeholder="ยืนยันรหัสผ่าน">
            </div>
            <div class="row mt-3">
                <div class="col-sm-6" >
                    <button type="button" onclick="nextForm(1)">ย้อนกลับ</button>
                </div>
                <div class="col-sm-6" style="text-align: right;">
                    <button type="submit">ลงทะเบียน</button>
                </div>
            </div>
    </form>
    <script src="/JSFILE/register.js"></script>
</body>
</html>